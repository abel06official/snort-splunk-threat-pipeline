# ----------------
# LOCAL RULES
# ----------------
# This file contains custom signatures authored for the SOC Project.

# 1. ICMP Flood Detection
# Detects standard ping requests targeting the internal network
alert icmp any any -> $HOME_NET any (msg:"ICMP TEST - PING DETECTED"; sid:1000001; rev:1;)

# 2. Nmap Stealth Scan Detection
# Detects TCP packets with only the SYN flag set (indicating a half-open connection attempt)
alert tcp any any -> $HOME_NET any (msg:"POTENTIAL NMAP SCAN DETECTED"; flags:S; sid:1000002; rev:1;)
